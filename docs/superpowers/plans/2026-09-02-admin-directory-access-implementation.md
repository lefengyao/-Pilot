# 管理员目录访问 Implementation Plan

## Task 1: 配置与权限核心

- [x] 为普通根目录固定 `terminal-workspace`，增加 `ADMIN_KEY` 配置校验。
- [x] 新增管理员令牌模块，覆盖时间安全比较、过期和失败限流。
- [x] 新增任意可访问目录解析器，并保持普通目录解析边界。

## Task 2: HTTP 与 WebSocket 授权

- [x] 新增 `POST /api/admin/unlock`。
- [x] 为目录接口增加普通/管理员两种解析模式。
- [x] 为 WebSocket 创建请求增加管理员令牌校验和管理员工作目录解析。

## Task 3: 网页解锁与目录选择

- [x] 增加管理员解锁弹窗和内存令牌状态。
- [x] 让目录选择器在解锁后浏览绝对路径，并在令牌失效时回到普通模式。
- [x] 管理员创建终端时携带令牌；普通流程保持兼容。

## Task 4: 验证与部署

- [x] 添加单元、HTTP、WebSocket 和浏览器回归测试。
- [x] 更新 `.env.example` 与 README 安全说明。
- [x] 重启 3000 服务并验证本机、局域网接口和页面流程。
