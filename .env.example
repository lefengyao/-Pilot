# 0.0.0.0 允许局域网设备访问；如需收窄监听范围，建议指定局域网适配器的 IP 地址。
HOST=0.0.0.0
PORT=3000
# 普通用户只能浏览此目录及其子目录；留空时默认为项目下的 terminal-workspace。
ALLOWED_ROOT=terminal-workspace
# 至少 32 个字符；留空则关闭管理员解锁。
ADMIN_KEY=
MAX_SESSIONS_PER_CLIENT=8
MAX_SESSIONS_TOTAL=64
MAX_MESSAGE_BYTES=65536
MAX_HISTORY_BYTES=524288
OUTPUT_BATCH_MS=16
# 单个文件上传上限，默认 2 GiB；可设置为 1 MiB 到 8 GiB 之间的字节数。
MAX_UPLOAD_BYTES=2147483648
